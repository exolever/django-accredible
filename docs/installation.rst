============
Installation
============

At the command line::

    $ easy_install django-accredible

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-accredible
    $ pip install django-accredible
